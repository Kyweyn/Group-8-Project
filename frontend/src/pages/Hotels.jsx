// Hotels.jsx - list of all hotels (GET /hotels).
// TODO: call the api here, for now it is only the page skeleton.
function Hotels() {
  return (
    <div>
      <h1>Hotels</h1>
      <p>The hotel list from the API goes here.</p>
    </div>
  );
}

export default Hotels;
